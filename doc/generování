
nezávazné-pouze koceptuální sada pravidel pro generování kódu

Generování:

funkce:
    předávání argumentů zásadně zásobníkem
    před zavoláním vtlačen nový frame

deklarace :
    deklarace budou odekorovány číslem-pro každý rozsah platnosti jiné číslo(pravděpodobně lineárně)

while:
    deklarace->rovnou
    zbytek do bufferu
    po ukončení->vypsání bufferu

expresions:
    pokud jde-přes zásobníkové instrukce
    co nejde- jednoduché integrované funkce pracující se zásobníkem->simulace zásobníkové instrukce
    po skončení výsledek na zásobníku

předdefinované funkce:
    v ifjcode
    zachovat stejné chování jako u ostatních funkcí->získání/vracení parametrů přes zásobník

    funkce write-1 parametr: počet argumentů




